export default function Options() {
  return (
    <>
      <div />
      {/* <div onClick={() => handleRestoreService()} className={`${isEdit ? 'visible op-100' : 'invisible op-0'} transition-all i-carbon-reset text-5 mr-3 cursor-pointer opacity-animation-3`} />
      <div onClick={() => setOpenConfirmModal(true)} className={`${isEdit ? 'visible op-100' : 'invisible op-0'} transition-all i-carbon-save text-5 mr-3 cursor-pointer opacity-animation-3`} />
      <div onClick={() => setOpenAddModal(true)} className={`${isEdit ? 'visible op-100' : 'invisible op-0'} transition-all i-carbon-task-add text-5 mr-3 cursor-pointer opacity-animation-3`} />
      <div onClick={() => setIsEdit(true)} className={`${isEdit ? 'i-carbon-edit-off' : 'i-carbon-edit'} text-5 cursor-pointer icon-tap-color mr-3 opacity-animation-3 `} /> */}
    </>
  );
}
